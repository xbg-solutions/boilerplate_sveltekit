{
  "name": "@xbg.solutions/bpsk-core",
  "version": "1.2.3",
  "description": "XBG Frontend Core - Base framework, types, stores, error handling, logging",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "type": "module",
  "files": ["lib", ".claude/skills", "scripts/copy-skills.cjs"],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf lib .claude",
    "prepublishOnly": "node scripts/bundle-skills.cjs && npm run build",
    "postinstall": "node scripts/copy-skills.cjs"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "svelte": "^5.0.0",
    "@sveltejs/kit": "^2.0.0"
  }
}
